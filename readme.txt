# zftest Authentication

## Endpoints

/index -> redirects to /dashboard 
/dashboard -> if user is not logged in, redirects to /auth/login

/auth/login -> allows user to log in
/auth/logout -> logs user out and redirects to login

/users/create -> allows to create a new user
